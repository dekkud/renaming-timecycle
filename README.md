# 🌦️ Renaming-Timecycle

**A Python script for automating the copying of timecycles for each weather option in GTA V.**

---

## 📌 Описание
Этот скрипт автоматизирует процесс синхронизации настроек графики (timecycles) для Grand Theft Auto V. Он берет один файл погоды и копирует его содержимое во все остальные погодные файлы, автоматически корректируя внутренние метаданные (теги XML), чтобы они соответствовали названию файла.

Это избавляет от необходимости вручную копировать и редактировать 17+ файлов при настройке тайм циклов.

## 🛠️ Функционал
* **Массовое копирование**: Дублирует структуру шаблона во все игровые погодные пресеты.
* **Авто-редактирование XML**: С помощью регулярных выражений меняет атрибут `name` в теге `<cycle>` на соответствующий (например, `EXTRASUNNY`, `CLEAR`, `FOGGY`).
* **Стандартизация**: Устанавливает параметр `regions` в значение `"2"` для всех файлов.

## 📂 Список обрабатываемой погоды
Скрипт обновляет следующие файлы:
`blizzard`, `clear`, `clearing`, `clouds`, `extrasunny`, `foggy`, `halloween`, `neutral`, `overcast`, `rain`, `rainhalloween`, `smog`, `snow`, `snowhalloween`, `snowlight`, `thunder`, `xmas`.

---

## 🚀 Инструкция по использованию

### 1. Подготовка
Убедитесь, что у вас установлен **Python 3.x**. Если нет, скачайте его с [python.org](https://www.python.org/).

### 2. Размещение
Поместите скрипт и ваш готовый файл `w_blizzard.xml` (который послужит шаблоном) в одну папку.

### 3. Запуск
Откройте данный Python файл. Иначе Вы можете открыть терминал или командную строку в этой папке и выполните:
```bash
python sync.py